builds:
  include:
  - '*.x86_64-linux.*'
  - darwinCongurations.*
servers:
  - configuration: server
    deployment:
      type: on-branch
      branch: main
  - configuration: server
    deployment:
      type: on-branch
      branch: main
